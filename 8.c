#include <stdio.h>
int main()
{
    int ;
    int   ;
    int arr[99] = {10, 20, 30, 40, 50};
    printf("Enetr the place you want to insert at = ");
    scanf(k, &k);
    printf("Enter the number you want to enter");
    scanf(value,&value);
    for(int i = k ; i > value ; i-- ) {
        arr[i] =  arr[i - 1];

    }
arr[value] =VID1

}
